<h1 class="subtitle">Write post</h1>

<%= render "form.html", changeset: @changeset,
                        action: post_path(@conn, :create) %>

<%= link "Back", to: dashboard_path(@conn, :index) %>
