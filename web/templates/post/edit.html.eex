<h1 class="subtitle">Edit post</h1>

<%= render "form.html", changeset: @changeset,
                        action: post_path(@conn, :update, @post) %>

<%= link "Back", to: dashboard_path(@conn, :index) %>
